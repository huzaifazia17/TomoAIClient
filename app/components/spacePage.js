'use client';

export default function spacePage({ spaceTitle }) {
    return (
        <div className="flex items-center justify-center h-full">
            <p className="text-gray-400">FREEEEEE</p>
        </div>
    );
}
